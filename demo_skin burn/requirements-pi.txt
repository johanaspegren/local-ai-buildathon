# Install on the Raspberry Pi (Pi 4 / Pi 5, 32 or 64-bit Raspberry Pi OS).
#
# tflite-runtime does not have a pip wheel for every Pi OS/Python combo.
# Try, in order:
#   1) pip install tflite-runtime
#   2) sudo apt install python3-tflite-runtime
#   3) If neither works, install the full "tensorflow" or "tensorflow-cpu"
#      wheel instead -- infer_pi.py automatically falls back to
#      tensorflow.lite.python.interpreter if tflite_runtime isn't importable.
pillow>=10.0
numpy>=1.26
